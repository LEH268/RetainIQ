# Required for every /api/ai/* endpoint to work. Without this, those
# endpoints return HTTP 503 instead of pretending to be AI-generated.
ANTHROPIC_API_KEY=

# Optional override, defaults to claude-sonnet-5
ANTHROPIC_MODEL=claude-sonnet-5